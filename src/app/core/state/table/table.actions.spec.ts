import * as fromTable from './table.actions';

// describe('loadFilters', () => {
//   it('should return an action', () => {
//     expect(fromTable.updateTableData({}).type).toBe('[Filter] Load Filters');
//   });
// });
